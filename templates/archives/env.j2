AMAZON_SQS_REGION="{{ aws_region }}"
AMAZON_KEY="{{ aws_access_key }}"
AMAZON_SECRET="{{ aws_secret_key }}"
SQS_ARCHIVES_URL="{{ aws_sqs_archives_url }}"

POSTGRES_HOST="localhost"
POSTGRES_DB="archive_production"
POSTGRES_USER="archiver"
RAILS_ENV="production"
