# headers that should be included with static cached content.

include /etc/nginx/conf.d/common-headers.conf;

add_header Cache-Control "public, max-age=31536000";
add_header Access-Control-Allow-Origin "*";
